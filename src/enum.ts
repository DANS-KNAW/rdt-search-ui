export enum FacetType {
	List = 'list',
	Map = 'map',

	// Charts
	Bar = 'bar',
	Date = 'date',
	Pie = 'pie',
}

export enum SortBy {
	Count = '_count',
	Key = '_key',
}

export enum SortDirection {
	Asc = 'asc',
	Desc = 'desc',
}
